# Gate P5 — Patterns

**Ngày:** 2026-08-26 · **Phase:** P5 — Patterns (Sub-01)
**P4:** OWNER PASS · **P5 technical:** PASS · **Owner UI:** một catalog như tab trước

**Catalog:** [https://staging.iflux.vn/design_system/sandbox/?section=patterns](https://staging.iflux.vn/design_system/sandbox/?section=patterns)

---

## 1. Patterns (6 — một trang catalog)

Chỉ compose primitives/components. Page Header = Pattern (Owner LOCK). Dữ liệu generic. 0 Admin runtime / business.

| Pattern | Compose | CSS riêng |
|---|---|---|
| Page Header | breadcrumb + title + description + action slot + optional stats (AR-001 §25) | `patterns/page-header/page-header.css` — REWRITE `.ix-page-title` |
| Data List | page-header + table + search + pagination + drawer (§26.1 sửa nhỏ) | — |
| Record Detail | page-header + card + timeline + action-bar | — |
| Entity Form | page-header + wizard + form + action-bar | — |
| Conversation | page-header + Chat Family | — |
| Order History | page-header + Timeline Family | — |

Tab Patterns nạp primitive + component + page-header CSS. Tab khác không nạp pattern CSS.

## 2. Governance

0 inline HTML · 0 `.ix-*` · media chỉ 768 trên page-header (title/actions hàng ngang từ md) · 0 admin dep · 0 màu hardcode.

## 3. DoD

```text
[x] Pattern chỉ compose primitives/components
[x] Page Header = Pattern, đúng AR-001 §25
[x] Catalog một trang — 6 pattern liệt kê như tab trước
[x] Dữ liệu generic
[x] Owner mở P6 (2026-08-26)
```
