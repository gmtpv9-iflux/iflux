# Gate P4 — Components

**Ngày:** 2026-08-26 · **Phase:** P4 — Components (Sub-01)
**P3:** OWNER PASS · **P4:** OWNER PASS (2026-08-26) · **P5:** MỞ

**Catalog:** [https://staging.iflux.vn/design_system/sandbox/?section=components](https://staging.iflux.vn/design_system/sandbox/?section=components)

Không duyệt lần lượt. Không `?family=`. 17 family liệt kê trên cùng một tab.

---

## 1. Families (17 — một trang catalog)

Thứ tự: card → stat → breadcrumb → form → table → pagination → drawer → action-bar → modal → toast → dropdown → tabs → search → timeline → wizard → chat → chart.

REWRITE `ix-*` → `ifx-*`. JS colocate, API `Ifx*`, `data-ifx-*`. 0 Admin runtime / business. Open/close = class `.is-open` (không `element.style`).

| Family | JS |
|---|---|
| Card | — |
| Stat | — |
| Breadcrumb | — |
| Form | — |
| Table | — |
| Pagination | `IfxPagination` |
| Drawer | `IfxDrawer` |
| Action Bar | — |
| Modal | `IfxModal` |
| Toast | `IfxToast` |
| Dropdown | `IfxDropdown` |
| Tabs | `IfxTabs` |
| Search | — |
| Timeline | — |
| Wizard | `IfxWizard` |
| Chat | `IfxChat` |
| Chart | `IfxChart` — màu semantic, listen `ifx-theme-change` |

Tab Components nạp CSS/JS của tab đó (giống Primitives). Tab khác không nạp.

Action Bar: không có artifact `ix-action-bar` — dựng từ AR-001 §27 + token `--ifx-space-48`.

Chart: adapter token-driven (CSS bars). ApexCharts vendor = khi sandbox P6 cần parity charts.html.

## 2. Governance

0 inline HTML · 0 `.ix-*` · media chỉ 768 trên form / wizard / chat · 0 admin dep · 0 màu hardcode trong `components/*.css`.

## 3. DoD

```text
[x] Catalog một trang — 17 family liệt kê như tab trước
[x] JS 0 Admin runtime / business
[x] Chart đổi theme đổi màu
[x] Đủ family phủ capability 9 patterns (P5 compose)
[x] Owner PASS catalog (2026-08-26)
```
