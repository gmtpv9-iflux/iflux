# Gate P4 — Components

**Ngày:** 2026-08-26 · **Phase:** P4 — Components (Sub-01)
**P3:** OWNER PASS · **P4 technical:** PASS · **Owner UI:** duyệt từng family trên sandbox

**Index:** [https://staging.iflux.vn/design_system/sandbox/?section=components](https://staging.iflux.vn/design_system/sandbox/?section=components)

Deep-link: `?section=components&family=<name>`

---

## 1. Families (17 — mỗi family một trang)

Thứ tự Plan: card → stat → breadcrumb → form → table → pagination → drawer → action-bar → modal → toast → dropdown → tabs → search → timeline → wizard → chat → chart.

REWRITE `ix-*` → `ifx-*`. JS colocate, API `Ifx*`, `data-ifx-*`. 0 Admin runtime / business. Open/close = class `.is-open` (không `element.style`).

| Family | URL | JS |
|---|---|---|
| Card | `family=card` | — |
| Stat | `family=stat` | — |
| Breadcrumb | `family=breadcrumb` | — |
| Form | `family=form` | — |
| Table | `family=table` | — |
| Pagination | `family=pagination` | `IfxPagination` |
| Drawer | `family=drawer` | `IfxDrawer` |
| Action Bar | `family=action-bar` | — |
| Modal | `family=modal` | `IfxModal` |
| Toast | `family=toast` | `IfxToast` |
| Dropdown | `family=dropdown` | `IfxDropdown` |
| Tabs | `family=tabs` | `IfxTabs` |
| Search | `family=search` | — |
| Timeline | `family=timeline` | — |
| Wizard | `family=wizard` | `IfxWizard` |
| Chat | `family=chat` | `IfxChat` |
| Chart | `family=chart` | `IfxChart` — màu semantic, listen `ifx-theme-change` |

Chỉ nạp CSS/JS family đang xem (+ primitives đã có).

Action Bar: không có artifact `ix-action-bar` — dựng từ AR-001 §27 + token `--ifx-space-48`.

Chart: adapter token-driven (CSS bars). ApexCharts vendor = khi sandbox P6 cần parity charts.html.

## 2. Governance

0 inline HTML · 0 `.ix-*` · media chỉ 768 trên form / wizard / chat · 0 admin dep · 0 màu hardcode trong `components/*.css`.

## 3. DoD

```text
[x] Mỗi component 1 trang demo
[x] JS 0 Admin runtime / business
[x] Chart đổi theme đổi màu
[x] Đủ family phủ capability 9 patterns (P5 compose)
[ ] Owner duyệt lần lượt trên catalog
```
