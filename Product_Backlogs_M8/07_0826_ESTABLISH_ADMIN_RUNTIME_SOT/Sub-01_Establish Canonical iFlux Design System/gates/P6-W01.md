# Gate P6-W01 — Bảng danh sách

> **moved 2026-08-28:** `design_system/references/patterns/<id>/` → `patterns/<id>/`. Staging URL `/patterns/<id>/`.


**Ngày:** 2026-08-26 · **Wave:** P6-W01 · **Owner:** PASS / LOCKED  
**Reusable gap:** 0 · Không còn PARTIAL / MISSING / WRONG OWNER unresolved

---

## A. Legacy reference

| | |
|---|---|
| Nhãn Owner | Bảng danh sách |
| Path | `Admin_Design_system/patterns/table-list.html` |
| Staging | https://staging.iflux.vn/Admin_Design_system/patterns/table-list.html |
| Vai trò | Visual / interaction / use-case baseline. **Không** runtime dependency. |

### Inventory (đọc markup + script)

1. **Layout** — Admin chrome + title, breadcrumb dưới title, KPI strip 1 card 4 cell, card (filter + toolbar + table + paginate).
2. **Typography** — `ix-page-title`, cell title/muted/price inline.
3. **Spacing** — `ix-mb-24`, card/filter/toolbar; nhiều inline.
4. **Grid/container** — không 12-col; filter row + widget-separator hàng ngang.
5. **Primitive** — button, chip, checkbox, select, Tabler.
6. **Component** — card, breadcrumb, filter-bar, table, toolbar, search, pagination, widget-separator.
7. **Pattern** — list + stats + table. Page header lệch AR-001 (title trước breadcrumb).
8. **Interaction** — `iflux-admin-ui.js` sort / select-all / search / paginate — `element.style`.
9. **Responsive** — không media trên page; chrome Admin.
10. **Theme** — `--ix-*` Admin bundle.
11. **Icon** — Tabler.
12. **Business** — Products / eCommerce SKU / $ KPI.
13. **Inline CSS** — ~27 `style=`.
14. **Legacy CSS/JS** — `iflux-admin-ui` + `admin-auth` + `admin-view-gate`.
15. **Không thuộc Global DS** — AppShell; auth/view-gate; eCommerce; `product-add.html`.

---

## B. Candidate reference

| | |
|---|---|
| Path | `patterns/table-list/` |
| Staging | https://staging.iflux.vn/patterns/table-list/ |
| Ownership | Consumer / reference. Không phải canonical capability source. |

---

## C. Capability matrix (final)

Status chỉ còn: RESOLVED_IN_P* / REFERENCE_LOCAL / WIDGET_MODULE_EXCLUDED / DROP_WITH_REASON

| Legacy capability | Required behavior | Layer | Canonical artifact | Final status |
|---|---|---|---|---|
| Tokens / Dark-Light | Theme semantic | P1 | generated CSS + `theme.js` | RESOLVED_IN_P1 |
| Container / grid / spacing / type | Page frame | P2 | `layout` `utilities` `typography` | RESOLVED_IN_P2 |
| Button / chip / icon / checkbox | Actions, status, select | P3 | `ifx-btn` `ifx-chip` Tabler `ifx-checkbox` | RESOLVED_IN_P3 |
| Avatar / entity cell | Icon + title + sub | P3 | `ifx-avatar-sm` + `ifx-inline-md` + typo | RESOLVED_IN_P3 |
| Card | Surface | P4 | `ifx-card` | RESOLVED_IN_P4 |
| Table visual + toolbar | List surface | P4 | `ifx-table` `ifx-table-toolbar` | RESOLVED_IN_P4 |
| Table sort / select / row visibility | Table state | P4 | `IfxTable` | RESOLVED_IN_P4 |
| Search | Query input | P4 | `ifx-search` + `[data-ifx-search]` | RESOLVED_IN_P4 |
| Pagination UI | Page buttons + event | P4 | `IfxPagination.render` / `ifx-page-change` | RESOLVED_IN_P4 |
| Form select 3 cột | Filters visual | P4 + P2 | `ifx-form-group` + `ifx-col-md-4` | RESOLVED_IN_P4 |
| Breadcrumb | Trail | P4 | `ifx-breadcrumb` | RESOLVED_IN_P4 |
| Stat (một KPI) | Single cell | P4 | `ifx-stat` | RESOLVED_IN_P4 |
| KPI shared strip + divider | 1 surface · n Stat · separator · stack/wrap | P4 | `ifx-stat-strip` + `ifx-stat-strip-divider` | **RESOLVED_IN_P4** |
| Page header | Breadcrumb → title → actions | P5 | `ifx-page-header` | RESOLVED_IN_P5 |
| Search + Table + Pagination wiring | query / page / page-size | P5 | `IfxDataList` | **RESOLVED_IN_P5** |
| Filter select apply (select → rows) | Legacy cũng không wire | — | visual selects | REFERENCE_LOCAL |
| App sidebar / navbar / brand / bell | Platform chrome | Platform | Admin AppShell | WIDGET_MODULE_EXCLUDED |
| Auth / view-gate | Admin runtime | — | — | DROP_WITH_REASON — không thuộc DS |
| eCommerce / product SKU / $ KPI | Domain copy | — | generic A-01… REC-* | WIDGET_MODULE_EXCLUDED |
| Add → `product-add.html` | W02 chưa mở | — | inert `button` | DROP_WITH_REASON — không link wave BLOCKED |
| Inline `element.style` table JS | Hack | — | class + token | DROP_WITH_REASON — rewrite class |
| Title trước breadcrumb | Lỗi AR-001 | — | không copy | DROP_WITH_REASON — canonical breadcrumb trước title |

---

## D / GAPS DISCOVERED THIS WAVE

| Gap | Classification | Correct owner | Action | Canonical artifact | Result |
|---|---|---|---|---|---|
| Shared KPI strip (`ix-widget-separator`) | reusable P4 | Stat family | Promote Stat Strip · compose Stat + Divider | `components/stat/stat-strip.css` | RESOLVED_IN_P4 |
| `IfxTable` ôm search + pager | WRONG OWNER | P5 Data-list | Split: Table state · Pagination UI · DataList compose | `table.js` + `pagination.js` + `data-list.js` | RESOLVED_IN_P5 |
| Search/sort/page generic | thiếu trước W01 | P4+P5 | Table + Pagination + DataList | như trên | RESOLVED_IN_P4 / P5 |
| Filter 3 cột | form-row 2 cột | P2 grid | Consume `ifx-col-md-4` | `ifx-grid` | RESOLVED_IN_P2 |
| AppShell | platform | Platform | Không clone | — | WIDGET_MODULE_EXCLUDED |
| Auth / eCommerce / inline style | drop | — | Không consume / rewrite | — | DROP_WITH_REASON |
| Filter select apply | page-only | Reference | Visual only (legacy parity) | — | REFERENCE_LOCAL |

**Unresolved reusable gap = 0.**

---

## E. Canonical DS changes (W01)

| Artifact | Layer | Why new / modify |
|---|---|---|
| `components/stat/stat-strip.css` | P4 | Existing `stat.css` = 1 KPI card. Strip = shared surface + n items + divider + responsive. Evidence reuse: `table-list.html` + `order-list.html`. Domain-independent. |
| `components/table/table.js` | P4 | **REWRITE ownership:** chỉ sort / select / `setVisible`. Không search, không pager. |
| `components/table/table.css` | P4 | State `th[data-ifx-sort]` · `tr.is-hidden` · `tr.is-selected`. |
| `components/pagination/pagination.js` | P4 | `render({page,pages,total})` + `ifx-page-change`. Không tbody. |
| `patterns/data-list/data-list.js` | P5 | Composer Search → Table → Pagination. Existing owner = data-list markup; JS không thể nhét vào table.js. |

---

## F. IfxTable exact responsibility

```text
IfxTable  = sort cột · row selection · visibility slice (is-hidden / is-selected)
            API: init · rows · setVisible
            event: ifx-table-sort

IfxSearch = presentation + native input  [data-ifx-search]
IfxPagination = render nút trang + ifx-page-change
IfxDataList (P5) = query · page · page-size · khớp hàng
```

```text
Search  ──┐
Filter  ──┼→ IfxDataList → Table.setVisible
Table   ──┤              → Pagination.render
Pagination┘
```

---

## G. Reference-local / exclusions

| File | Việc được phép |
|---|---|
| `index.html` | Composition + demo data (10 hàng) |
| `page.js` | Theme toggle + `IfxDataList.init()` |

0 CSS local.

**Widget/module:** không chart. eCommerce = WIDGET_MODULE_EXCLUDED.

---

## H. Candidate dependencies (consume only)

P1 tokens + `theme.js`  
P2 foundation  
P3 button · chip · avatar · icon  
P4 card · stat · **stat-strip** · breadcrumb · form · table · **IfxTable** · search · **IfxPagination**  
P5 page-header · **IfxDataList**

0 legacy · 0 Admin runtime · 0 `.ix-*` · 0 inline · DS không phụ thuộc `references/`.

---

## I. Responsive matrix

| Viewport | Dark | Light | Ghi chú |
|---|---|---|---|
| 360 | PASS | PASS | Strip stack + divider ngang; table scroll |
| 480 | PASS | PASS | strip vẫn stack |
| 768 | PASS | PASS | Strip hàng + divider dọc, wrap nếu hẹp; filter 3 cột |
| 1024 | PASS | PASS | Strip 4 ô một hàng |
| 1280 | PASS | PASS | container-max |
| 1440 | PASS | PASS | 2xl |

---

## J. Theme matrix

Dark/Light = canonical tokens. 0 theme logic local.

---

## K. Owner result

```text
[x] PASS / LOCKED
[ ] REJECT
```

W02 không mở từ gate này. Tiếp theo: Sandbox Normalization (`gates/P6-SANDBOX.md`).
