# Gate P6-W01 — Bảng danh sách

**Ngày:** 2026-08-26 · **Wave:** P6-W01 · **Owner:** chờ review

---

## A. Legacy reference

| | |
|---|---|
| Nhãn Owner | Bảng danh sách |
| Path | `Admin_Design_system/patterns/table-list.html` |
| Staging | https://staging.iflux.vn/Admin_Design_system/patterns/table-list.html |
| Vai trò | Visual / interaction / use-case baseline. **Không** runtime dependency. |

### Inventory (đọc markup + script, không đoán screenshot)

1. **Layout** — Admin chrome `ix-root` → sidebar + navbar + `ix-content`. Content: title, breadcrumb dưới title, KPI strip 1 card 4 cell, card (filter 3 select + toolbar + table + paginate).
2. **Typography** — `ix-page-title`, cell title 13/500, muted 11px, price 500.
3. **Spacing** — `ix-mb-24`, card padding, filter row, toolbar gap. Nhiều khoảng cách inline.
4. **Grid/container** — không 12-col; filter row + widget-separator hàng ngang.
5. **Primitive** — button (outline/primary/icon/sm), chip, checkbox, select, icon Tabler.
6. **Component** — card, breadcrumb, filter-bar, table, table-toolbar, search, pagination, stat/widget-separator.
7. **Pattern** — page list + stats + table. Page header lệch AR-001 (title trước breadcrumb).
8. **Interaction** — `iflux-admin-ui.js`: sort `th[data-ix-sort]`, select-all, search `data-ix-search`, paginate `data-per-page="5"`. Toàn bộ dùng `element.style`.
9. **Responsive** — không media riêng trên page; chrome Admin. Mobile baseline yếu.
10. **Theme** — consume `--ix-*` Admin bundle.
11. **Icon** — Tabler (`ti-table`, `ti-device-mobile`, …).
12. **Business** — Products / eCommerce / iPhone / Nike / FXZ SKU / $ KPI.
13. **Inline CSS** — ~27 `style=` (chevron 12px, icon 44×44 / 36×36, chip pad, flex cell, price color, action gap).
14. **Legacy CSS/JS** — `iflux-admin-ui.css/.js`, `admin-auth.js`, `admin-view-gate.js`. `data-ix-view-api="/admin/guides/patterns_table"`.
15. **Không thuộc Global DS** — AppShell sidebar/navbar/brand/bell; auth/view-gate; eCommerce copy; link `product-add.html`.

---

## B. Candidate reference

| | |
|---|---|
| Path | `design_system/references/patterns/table-list/` |
| Staging | https://staging.iflux.vn/design_system/references/patterns/table-list/ |
| Ownership | Consumer / reference. Không phải canonical capability source. |

---

## C. Capability mapping

| Legacy capability | Required behavior | Canonical layer | Existing artifact | Status |
|---|---|---|---|---|
| Tokens / Dark-Light | Theme semantic | P1 + adapter | generated CSS + `theme.js` | AVAILABLE |
| Container / grid / spacing / type | Page frame, 12-col, stack | P2 | `layout.css` `utilities.css` `typography.css` | AVAILABLE |
| Button / chip / icon / checkbox | Actions, status, select | P3 + form | `ifx-btn` `ifx-chip` Tabler `ifx-checkbox` | AVAILABLE |
| Avatar / entity thumb | Icon + title + sub | P3 | `ifx-avatar-sm` + `ifx-inline-md` + typo | AVAILABLE |
| Card | Surface | P4 | `ifx-card` | AVAILABLE |
| Table + toolbar | List surface | P4 | `ifx-table` `ifx-table-toolbar` | AVAILABLE |
| Search | Filter rows by text | P4 | `ifx-search` | AVAILABLE |
| Pagination UI | Page buttons | P4 | `ifx-pagination` `ifx-page-btn` | AVAILABLE |
| Form select / label | 3 filters | P4 + P2 | `ifx-form-group` `ifx-select` + `ifx-col-md-4` | AVAILABLE |
| Breadcrumb | Trail | P4 | `ifx-breadcrumb` | AVAILABLE |
| Stat | KPI cell | P4 | `ifx-stat` + trend + chip | AVAILABLE |
| Page header | Breadcrumb → title → actions | P5 | `ifx-page-header` | AVAILABLE |
| Data-list composition | Header + filters + table + pager | P5 | compose (catalog data-list) | AVAILABLE |
| Table search / sort / select-all / client page | Generic table behavior | P4 | **không có trước W01** (`IfxPagination` chỉ UI) | **MISSING → promoted** `IfxTable` |
| KPI 4-cell một card (`ix-widget-separator`) | Strip 1 surface 4 ngăn | — | `ifx-stat` + `ifx-grid` (4 card riêng) | **PARTIAL** |
| Filter apply (select → rows) | Legacy cũng không wire JS | — | visual only | REFERENCE-LOCAL |
| App sidebar / navbar / brand / bell | Platform chrome | Platform | Admin AppShell | **WRONG OWNER** |
| Auth / view-gate | Runtime | Admin | — | **DROP** |
| eCommerce / product SKU / $ KPI | Domain copy | Module | — | **DROP** (generic A-01… REC-*) |
| Add → `product-add.html` | Sang W02 | W02 | inert `button` | DROP / chưa mở W02 |
| Inline `element.style` table JS | Ẩn/chọn/pager | Hack | class `is-hidden` / `is-selected` | **DROP** |

---

## D. Gap discovered

Xem bảng GAPS bên dưới.

---

## E. Canonical DS changes

| Artifact | Layer | Why cannot modify existing only |
|---|---|---|
| `components/table/table.js` (`IfxTable`) | P4 | Hành vi search/sort/select/page sống ở `iflux-admin-ui.js`. `table.css` chỉ visual. `IfxPagination` không biết tbody. |
| `components/table/table.css` | P4 | Thêm contract state: `th[data-ifx-sort]`, `tr.is-hidden`, `tr.is-selected`. Token `--ifx-action-primary-soft`. |

Không thêm `filter-bar.css`. Không thêm widget-separator vào Global DS (PARTIAL — 4 `ifx-stat` trong grid).

---

## F. Reference-local code

| File | Việc được phép |
|---|---|
| `index.html` | Composition + demo data (10 hàng generic) |
| `page.js` | Theme toggle harness + `IfxTable.init()` |

0 CSS local. 0 button/table/pagination/form/theme tự viết.

---

## G. Widget / module exclusions

Không có chart/widget market. eCommerce copy = DROP.

---

## H. Dependency check

| Check | Result |
|---|---|
| Legacy CSS/JS import | 0 |
| iframe / redirect legacy | 0 |
| Admin runtime (`admin-auth`, view-gate, `/api/admin`) | 0 |
| `.ix-*` / `--ix-*` | 0 |
| Inline `style=` | 0 |
| Canonical DS → references | 0 (chiều ngược) |

---

## I. Responsive matrix

Mobile-first canonical. Desktop giữ intent list-page. Không copy lỗi chrome Admin.

| Viewport | Dark | Light | Ghi chú |
|---|---|---|---|
| 360 | PASS | PASS | Stats stack 1 cột; table scroll ngang (`ifx-table-wrap`) |
| 480 | PASS | PASS | sm — vẫn 1 cột stat |
| 768 | PASS | PASS | md — stat 2 cột; filter 3 cột; header bar hàng ngang |
| 1024 | PASS | PASS | lg — stat 4 cột |
| 1280 | PASS | PASS | container-max |
| 1440 | PASS | PASS | 2xl |

---

## J. Theme matrix

| | Dark | Light |
|---|---|---|
| Tokens + `IfxTheme` | PASS | PASS |
| Theme logic local | Không | Không |

---

## K. Owner result

```text
[ ] PASS  → lock W01 → mở P6-W02
[ ] REJECT → sửa W01 · không mở W02
```

---

## GAPS DISCOVERED THIS WAVE

| Gap | Classification | Correct owner | Action | Canonical artifact | Result |
|---|---|---|---|---|---|
| Table search / sort / select-all / client page | MISSING | P4 Table | Promote `IfxTable` — class state, 0 `element.style` | `components/table/table.js` + state CSS | Consumed |
| KPI strip 1 card 4 cell | PARTIAL | P4 Stat (nếu Owner muốn parity strip) | Không local-hack. Consume `ifx-stat` + grid | `ifx-stat` | 4 card riêng — chờ Owner |
| `ifx-form-row` chỉ 2 cột | PARTIAL | P4 Form hoặc P2 grid | Consume `ifx-col-md-4` — không tạo filter-bar | `ifx-grid` | Consumed |
| Page header title-before-breadcrumb | WRONG vs AR-001 | P5 | Không copy lỗi legacy | `ifx-page-header` | Breadcrumb trước title |
| AppShell chrome | WRONG OWNER | Platform | Harness theme only | — | Excluded |
| Auth / view-gate | DROP | Admin runtime | Không consume | — | Excluded |
| eCommerce Products | DROP | Module | Generic demo data | — | Excluded |
| Inline style table JS | DROP | — | Rewrite class + token | `IfxTable` | Dropped |
| Filter select apply | REFERENCE-LOCAL | Page | Legacy cũng không wire | visual selects | Giữ visual |
