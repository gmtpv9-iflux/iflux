# Gate P2 — Layout + DS + Sidebar visual

| Field | Value |
| --- | --- |
| Phase | P2 |
| Ngày | 2026-08-31 |
| Parent | [`05_Plan.md`](../05_Plan.md) rev 2 |
| Scope | PLAN-02 · PLAN-03 · PLAN-06 |

## Local AC

| AC | Evidence |
| --- | --- |
| Hierarchy Sidebar \| Main Area | `document.html`: sidebar rồi `main[data-admin-main-area]` chứa Header region. Cấm Header full-width trên Sidebar |
| Header width = Main Area | Header nằm trong `main`, không sibling ngang hàng Sidebar phía trên |
| Sticky cả Header region | [`layout.css`](../../../../../platform/admin/shell/layout.css) `[data-admin-header-region] { position: sticky }` |
| Scroll owner = Host | `[data-ix-admin-page-host] { overflow-y: auto }` · `.ix-content` trong Host không còn scroll owner |
| Page Header slot trống | `[data-admin-page-header]:empty { display: none }` · boot không paint title/bc |
| DS consume, 0 copy | Link `page-header.css` / `nav.css` / tokens. `components.css` không có `.ifx-page-header` |
| Sidebar `syncActive` + `.ifx-nav-*` | `iflux-admin-app-shell-sidebar.js` — giữ `ix-menu-item` + thêm `ifx-nav-item` / icon / label |
| `data-theme` | `ensureCanonicalAssets` set `light` nếu thiếu |
| ALLOWLIST | vẫn `[]` — 0 rewrite / 0 extract TARGET |

## Chưa làm (đúng Plan)

- Không convert page / không `[data-admin-page]` trên `users/list.html`
- Không `setPageHeader` paint
- Không F3H remount
- Không cleanup

## Visual ownership (Owner test)

| Surface | Owner visual | Được giữ |
| --- | --- | --- |
| Header / Sidebar / Page Header generic | Canonical DS (`ifx-nav-*` · `ifx-chip` · `ifx-avatar` · `ifx-btn` · `ifx-page-header`) | `data-*` hooks · `IfluxAdminAppShell` |
| Composition / sticky / scroll / size | `platform/admin/shell/layout.css` + platform tokens | |
| `ix-*` chrome | Không còn paint active trên 3 surface AppShell | class hook runtime nếu còn |

## Owner test Staging (Plan §5 · P2)

Cần push `staging` rồi Owner kiểm:

1. Desktop 1024+: Header không phủ Sidebar.
2. Cuộn trang dài: Global Header (+ region) giữ; chỉ Host cuộn.
3. Nav nội bộ: Header container + Sidebar không recreate (`BOOT_ID`).
4. Route AS-IS: không title AppShell chồng title local.

**Local PASS. CONTINUE P3 chỉ sau Owner test Staging PASS.**
