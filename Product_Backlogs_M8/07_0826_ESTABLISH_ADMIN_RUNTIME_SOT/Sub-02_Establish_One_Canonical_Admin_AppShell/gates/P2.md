# Gate P2 — Layout + DS + Sidebar visual (Modify existing)

| Field | Value |
| --- | --- |
| Phase | P2 |
| Ngày | 2026-08-31 |
| Parent | [`05_Plan.md`](../05_Plan.md) rev 2 |
| Scope | PLAN-02 · PLAN-03 · PLAN-06 |
| Cách làm | **Modify owner hiện có.** 0 file CSS/JS mới. 0 neutralize. 0 ép theme. |

## Impact Analysis (CG-005)

| | |
| --- | --- |
| Feature | P2 layout sticky/scroll + Sidebar/Header consume DS |
| Current owner | `components.css` (`.ix-layout` / `.ix-main` / `.ix-navbar` / host) · `iflux-admin-app-shell-header.js` · `iflux-admin-app-shell-sidebar.js` · `semantic/aliases.css` · `iflux-admin-ui.css` |
| Files | Chỉ các file trên + `document.html` (slot) + `iflux-theme.js` (selector chip) + 1 dòng skip slot trong `iflux-admin-app-shell.js` |
| Consumers | Mọi page AS-IS load `iflux-admin-ui.css` + Header/Sidebar JS |
| Decision | **Modify.** Không tạo `layout.css` / `entry.css`. Không inject link. Không `data-theme`. |

Layout Sidebar \| Main Area **đã có** trong `components.css` (sidebar fixed, header trong `main`, navbar đã `sticky`). Lần P2 trước tạo `layout.css` là nhân bản — không làm lại.

## Không tạo

- `platform/admin/shell/layout.css`
- `platform/admin/shell/entry.css`
- `ensureCanonicalAssets` / `ensureLayoutFrame`
- `@import` overlay vào ui.css để che `ix-*`
- Cache-bust 97 HTML
- Dual class `ix-menu-item` + `ifx-nav-item` trên cùng node

## Đã sửa (owner cũ)

| Owner | Đổi |
| --- | --- |
| `components.css` | Host = scroll (`overflow-y: auto`). `.ix-content` hết scroll owner. Slot `[data-admin-page-header]:empty` ẩn. Collapse/submenu nhận `.ifx-nav-item`. |
| `iflux-admin-ui.css` | `@import` **file DS canonical** (nav/chip/avatar/button/page-header/breadcrumb). Không copy rule. |
| `aliases.css` | Map `--ifx-text-*` / `--ifx-bg-*` / chip-button radius → `--color-*` sẵn có. |
| `iflux-admin-app-shell-sidebar.js` | Item = `.ifx-nav-item` / `.ifx-nav-icon` / `.ifx-nav-label` / `.is-active`. Structure submenu/group giữ. |
| `iflux-admin-app-shell-header.js` | Chip/avatar/logout = `ifx-*`. Slot Page Header trống, không paint title/bc. |
| `iflux-admin-app-shell.js` | `collectPageOwned` bỏ qua `[data-admin-page-header]`. ALLOWLIST vẫn `[]`. |
| `iflux-theme.js` | Tìm chip theo `[data-ix-admin-env]`. Không đổi default dark. |
| `document.html` | Slot trống + avatar `ifx-*`. Không serve (allowlist rỗng). |

## Local AC

| AC | Evidence |
| --- | --- |
| Hierarchy Sidebar \| Main Area | `components.css` `.ix-layout` flex; header trong `main.ix-main`. 0 Header full-width |
| Sticky Header | `.ix-navbar { position: sticky }` đã có. Slot page-header sticky dưới navbar khi không empty |
| Scroll = Host | `[data-ix-admin-page-host] { overflow-y: auto }` |
| Page Header không paint | Slot empty `display: none`. `fillBreadcrumb` chỉ `#adm-page-bc` leftover page |
| DS consume | `@import` path `/design_system/...`. 0 copy rule vào `ix-*` |
| Sidebar `ifx-nav-*` | `itemHtml` / `parentHtml` — 0 `ix-menu-item` trong sidebar.js |
| Theme | Không ghi `data-theme`. `IfluxTheme` + `theme.css` `:root` dark giữ |
| ALLOWLIST | `[]` — 0 rewrite |

## P0/P1 giữ

`IfluxAdminAppShell` = 1 · V2 = 0 · resolver/guard inactive · Auth ngoài shell.

**Local xong. Owner hard refresh Staging.** CONTINUE P3 chỉ sau Owner PASS.
