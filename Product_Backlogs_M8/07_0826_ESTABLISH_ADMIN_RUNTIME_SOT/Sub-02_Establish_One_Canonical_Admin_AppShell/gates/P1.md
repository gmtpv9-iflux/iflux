# Gate P1 — Runtime + infra + Auth (chưa bật kênh canonical)

| Field | Value |
| --- | --- |
| Phase | P1 |
| Ngày | 2026-08-31 |
| Parent | [`05_Plan.md`](../05_Plan.md) rev 2 |
| Scope | PLAN-01 · PLAN-05(guard code) · PLAN-08(Auth rào) · PLAN-09(infra) |

## ALLOWLIST

```text
platform/admin/shell/canonical-route-allowlist.json  = []
CANONICAL_ROUTE_ALLOWLIST (app-shell.js)             = []
```

0 `routeKey` PAGES. 0 rewrite PUBLIC→shell-only.

## AC

| AC | Evidence |
| --- | --- |
| `IfluxAdminAppShellV2` = 0 | Grep repo JS = 0. Cùng `IfluxAdminAppShell` |
| ALLOWLIST = ∅ | JSON + JS array rỗng |
| 0 PAGES rewrite shell-only | Express chỉ `sendFile` shell khi key ∈ allowlist. Nginx staging **không** include `admin-canonical-rewrite.inc` |
| 0 reject thiếu marker trên route sống | `useCanonical` false khi list rỗng → AS-IS `collectPageOwned` |
| Navigate AS-IS 2 page | `users/list.html` + `dashboard/index.html` vẫn full document + `main.ix-main`. Không `[data-admin-page]` |
| Auth không AppShell | `login.html` = `.ix-auth-root`, 0 sidebar/layout. Express skip `/admin/login`. `isAppBoundary` giữ |
| `BOOT_ID` giữ identity | Không fork runtime; `bootId` / `navigate` / Host giữ |
| Không fallback `.ix-content` | app-shell không query `.ix-content` để extract |

## Infra sẵn (chưa kích hoạt)

- [`platform/admin/shell/document.html`](../../../../../platform/admin/shell/document.html) — `[data-admin-shell-document]`
- Resolver: `publicHref → matchPath → file → /Admin_Design_system/app/{file}`
- Guard TARGET: reject shell / thiếu `[data-admin-page]` — **chỉ** khi allowlist
- [`infra/staging-1/admin-canonical-rewrite.inc`](../../../../../infra/staging-1/admin-canonical-rewrite.inc) — 0 `location`

## Invariant §3.5

P1: ALLOWLIST ∅ → không fetch TARGET, không rewrite PAGES.

## PASS

Mọi AC local. Kênh canonical **chưa** bật.

**CONTINUE:** P2
