# Gate P0 — Baseline inventory

| Field | Value |
| --- | --- |
| Phase | P0 |
| Ngày | 2026-08-31 |
| Mode | READ ONLY — không sửa runtime |
| Parent | [`05_Plan.md`](../05_Plan.md) rev 2 OWNER APPROVED |

## Count (source 2026-08-31)

| Mục | Số | Ghi chú vs Audit |
| --- | --- | --- |
| Runtime AppShell | 1 (`IfluxAdminAppShell`) | Khớp |
| `IfluxAdminAppShellV2` | 0 | Khớp |
| `BOOT_ID` | Có — `data-ix-admin-shell-boot` · persist Header/Sidebar trên internal nav | Khớp Audit §4 |
| `ix-layout` HTML `app/**` | 97 | Khớp 97 chrome copy |
| `#adm-page-bc` HTML | 95 | Audit 97 = 94 app + hub + extras; delta = đếm file có id |
| Page Header impl | 5 | Khớp Audit §8 |
| F8 `PAGES.file = null` | 5 | hub · design-system · table-list · form-add · charts |
| F3H | `#req-search` partnership · `#adm-search` features + bugs | Khớp |
| Auth | `/Admin_Design_system/auth/login.html` · PUBLIC `/admin/login` | EC-02 · không AppShell |
| PAGES object keys | 115 | alias + legacy |
| Unique identity | 94 | |
| File-backed identity | 89 | Audit ~87 + leftover; delta ghi nhận |
| `platform/admin` layout HTML/JS | 0 (tokens only, trước P1) | Khớp |

## Canary (Plan default)

| | |
| --- | --- |
| `routeKey` | `users-list` |
| `PAGES.file` | `users/list.html` |
| PUBLIC (pathFor / hrefFor) | `/admin/users/list` |
| Legacy slug | `/admin/khach-hang/list` → cùng identity |
| Fragment path | `/Admin_Design_system/app/users/list.html` |

Xác định đủ → P0 không STOP.

## Persist AS-IS (code)

Internal nav: `navigate` → `pageEpoch++` · Host thay · `afterPageIn` `syncActive` — không `Header.render` / `Sidebar.render`. Refresh = document mới = recreate.

## Serve `/admin/*` (đúng chỗ rewrite)

| Môi trường | Owner |
| --- | --- |
| Staging | Express `mountAdminUi` (`backend/src/app.js`) qua nginx `@admin_ia` |
| Production nginx | rewrite file HTML đầy đủ (AS-IS) |

P1 dual-path: Express + allowlist file. Nginx staging **không** thêm rule PAGES sống ở P0/P1.

## PASS

Inventory đủ P3 canary + P6 batch. Canary `routeKey` + file xác định.

**CONTINUE:** P1
