# Phase Gate — P6 (A/B/C)

Status: **PASS**

Measured: 2026-08-19 · Staging `iflux-api-staging` · release `470d02f`  
BRD / SoT / Solution / P0–P5 gates: không sửa.

Authority: Plan P6 · BRD §6 · Solution §9 / §33–§34 · Owner mở P6.

Test profiles (đã **RETIRE** ngay sau enqueue): `media-p6-probe` · `media-p6-spec`.  
Required set ACTIVE vẫn đúng 5 seed. Không để profile test ACTIVE sót.

---

## Implemented

- Catalog `media`: `media.profile.view` · `media.profile.manage` · `media.profile.regenerate` · `media.cleanup`.
- API trên router media hiện có: `GET /profiles` (mọi status) · `POST /profiles` · `PATCH /profiles/:key` · `POST /profiles/:key/regenerate`.
- `createImageProfile` / `updateImageProfile`: lifecycle DRAFT → ACTIVE → DEPRECATED → RETIRED; spec DRAFT sửa tại chỗ; spec ACTIVE → version mới.
- ACTIVE → `enqueueGenerateForProfile` (không `generateDerivative` trong HTTP).
- Reserved: `media-detail` · `media-thumbnail` · `card-lg` bị từ chối.
- `listImageProfiles()` (ingest/VERIFY) chỉ profile **ACTIVE** + version mới nhất.
- Page **Quy chuẩn hình ảnh**: list · tạo · sửa spec · đổi trạng thái · regen. **Không** danh sách asset.
- Nav **Quản lý Thư viện** trên **Quản lý giao diện**; slug `/admin/thu-vien/quy-chuan-hinh-anh`.

---

## Slices

| Req ID | A | B | C | Status |
|---|---|---|---|---|
| BRD-06.01–.04 Admin tập trung / Thư viện / trên giao diện | page + nav registry | — | slug 200 · group trước giao diện | **PASS** |
| BRD-06.05 không list asset | page không gọi `/assets` | — | HTML không có asset table | **PASS** |
| BRD-06.06 / AC-19 tạo profile | `POST /profiles` + v1 | row `media-p6-probe` v1 | 201 · 5ms · DRAFT · enqueue null | **PASS** |
| BRD-06.07 xem | `GET /profiles` admin list | 5 seed + test rows | 200 · đủ status | **PASS** |
| BRD-06.08 / BR-01.09 lifecycle | PATCH status 4 trạng thái | probe RETIRED · spec RETIRED | DRAFT→ACTIVE→RETIRED | **PASS** |
| BRD-06.09 sửa spec/version | PATCH spec DRAFT in-place | `max_width` 240→360 · vẫn v1 | 200 · 5ms | **PASS** |
| BRD-06.10 / BRD-08.01 kích hoạt async | ACTIVE → enqueue only | 6 GENERATE queued rồi cancelled | PATCH ACTIVE **19ms** · `enqueued: 6` | **PASS** |
| Reserved keys | `assertProfileKey` | 0 row reserved | 400 cả 3 key | **PASS** |
| RBAC 4 key | catalog flatten | `admin_permissions` 4 key | bootstrap seed live | **PASS** |

P5b metrics / P7 adapters: **không** mở.

---

## Evidence A — Static

```text
GET    /api/admin/media/profiles
POST   /api/admin/media/profiles
PATCH  /api/admin/media/profiles/:key
POST   /api/admin/media/profiles/:key/regenerate
```

ACTIVE → `enqueueGenerateForProfile` → job `queued`. Không `generateDerivative` trên Admin HTTP.

`listImageProfiles()` = `p.status = 'ACTIVE'` + latest version. Create DRAFT không tăng required set.

Reserved: `media-detail` · `media-thumbnail` · `card-lg`.

Files: `media.service.js` · `media.routes.js` · `permission-catalog.js` · `quy-chuan-hinh-anh.html` · `.js` · nav/routes/rbac.

---

## Evidence B — Database (`iflux_staging`)

```text
ACTIVE required = media-body · media-card · media-compact · media-hero · media-og
media-p6-probe  RETIRED  v1  320×auto webp q80
media-p6-spec   RETIRED  v1  max_width 360 q81  (sửa DRAFT in-place)
reserved keys   0 row
admin_permissions: media.profile.view · manage · regenerate · media.cleanup
```

ACTIVE probe: 6 GENERATE `queued` (assets có master). Sau RETIRE: 6 job `cancelled` — không để 6th profile vào required set.

```sql
SELECT profile_key, status FROM media_image_profiles ORDER BY 1;
SELECT key FROM admin_permissions WHERE key LIKE 'media.%';
```

---

## Evidence C — Runtime Staging

| Case | Result |
|---|---|
| `GET /profiles` | 200 · 8ms · 5 seed ACTIVE |
| `POST media-detail` / `media-thumbnail` / `card-lg` | 400 · reserved |
| `POST media-p6-probe` DRAFT | **201 · 5ms** · v1 · enqueue null |
| `PATCH ACTIVE` | **200 · 19ms** · `enqueued: 6` · không chờ generate |
| `PATCH RETIRED` | 200 · 4ms · enqueue null |
| Spec DRAFT 240→360 | 200 · 5ms · vẫn v1 |
| Page `https://staging.iflux.vn/admin/thu-vien/quy-chuan-hinh-anh` | 200 · form tạo · 4 trạng thái · caption không phải thư viện file · không list asset |
| Required ACTIVE sau test | **5 seed** |

---

## Decision

**P6 = PASS.** P5b / P7 chưa mở.
